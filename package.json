{
  "name": "tank-game",
  "version": "0.0.1",
  "description": "An attempt at a Browsergame",
  "scripts": {
    "build": "echo Must build workspaces!",
    "start": "npm run build -w=client && npm run start -w=server",
    "dev": "npx tsnd --respawn --clear src/index.ts"
  },
  "author": "Temm",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.2.3"
  },
  "workspaces": [
    "server",
    "client"
  ]
}
